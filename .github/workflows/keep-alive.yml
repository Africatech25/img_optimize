name: Keep Render API Alive

on:
  schedule:
    # Ping toutes les 5 minutes (limite minimale de GitHub)
    - cron: '*/5 * * * *'

  # Permet de déclencher manuellement depuis l'interface GitHub
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping API Health Endpoint
        run: |
          echo "Pinging API at $(date)"

          # URL de l'API Render
          API_URL="${{ secrets.RENDER_API_URL }}/api/health"

          # Effectuer le ping avec timeout de 30s
          response=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 "$API_URL")

          if [ "$response" -eq 200 ]; then
            echo "✅ API is alive! Status: $response"
          else
            echo "⚠️  API returned status: $response"
            exit 1
          fi

      - name: Log Ping Result
        if: always()
        run: |
          echo "Ping completed at $(date)"
          echo "Next ping in 5 minutes"
